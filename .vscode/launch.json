{
  "version": "2.0.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug executable",
      "cwd": "${workspaceFolder}/server",
      "cargo": {
        "args": ["build", "--manifest-path=server/Cargo.toml"]
      },
      "args": []
    }
  ]
}
